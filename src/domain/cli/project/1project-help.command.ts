import { CommandType } from "../command/command.data-type.ts";

export class _1projectHelpCommand implements CommandType {
  async handler(_args: string[]): Promise<void> {
    throw new Error(`Not implemented yet`);
  }
}
